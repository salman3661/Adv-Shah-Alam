# Advocate Md. Shah Alam Law Chambers — www.advmdshahalam.me
# Updated: 2026-07-12
# Strategy: Allow crawling of ALL public content. Duplicate/redirected URLs
# are handled via 301 redirects in vercel.json (NOT robots.txt blocking).
# Sitemap contains only canonical www URLs (non-redirected).
# advmdshahalam.me → www.advmdshahalam.me redirect handled by Vercel (301).

User-agent: *
Allow: /

# --- Block admin, API, and utility paths ---
Disallow: /admin/
Disallow: /api/
Disallow: /*?*          # Block URL query strings to avoid duplicate crawls
Disallow: /#            # Block fragment-only URLs

# ── Googlebot: no crawl rate limit (Googlebot self-manages) ──
User-agent: Googlebot
Allow: /
Disallow: /admin/
Disallow: /api/

# ── Bingbot: optimized crawl rate (Bing respects Crawl-delay) ──
User-agent: Bingbot
Allow: /
Disallow: /admin/
Disallow: /api/
Crawl-delay: 2

# ── Canonical Sitemap (www only) ──
Sitemap: https://www.advmdshahalam.me/sitemap.xml

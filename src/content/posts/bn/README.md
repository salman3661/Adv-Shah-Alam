This folder holds new Bangla blog posts created via the Decap CMS editor.

Each file is a JSON document created when you click "+ New বাংলা পোস্ট" in /admin.

Existing posts (before CMS setup) remain in src/data/blogPostsBn.js and are still served normally.
